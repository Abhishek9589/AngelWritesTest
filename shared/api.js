/**
 * Shared code between client and server
 * Useful to share JSDoc typedefs between client and server
 */

/**
 * @typedef {Object} DemoResponse
 * @property {string} message
 */

// Example runtime export (no-op placeholder for typings)
export {};
